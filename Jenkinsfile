pipeline {
    agent any

    stages {
        stage('testing multi') {
            steps {
                dir('Demo') {
                    sh 'echo test'
                }
            }
        }
    }
}